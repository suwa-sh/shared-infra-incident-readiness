PyYAML>=6.0,<7.0
# [format-nongpl] extra brings rfc3339-validator so "format": "date-time"
# actually validates the incident notification timestamps (otherwise no-op).
jsonschema[format-nongpl]>=4.21,<5.0
