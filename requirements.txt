# Dependencies are declared in pyproject.toml (the single source of truth).
# Installing this file installs the package together with all declared
# dependencies (including the pinned overlay-scoring-skeleton engine).
.
